source "https://rubygems.org"
# For a different version of Jekyll, change it below, save the
# file and run `bundle install`, then run Jekyll with `bundle exec`:
#     bundle exec jekyll serve

#gem "jekyll", "~> 3.7.3"
# For GitHub Pages, remove the "gem "jekyll"" above and
# uncomment the line below. To upgrade, run `bundle update github-pages`
gem "github-pages", group: :jekyll_plugins
